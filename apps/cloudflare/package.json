{
  "name": "@remote-hands/cloudflare",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/worker.js",
  "types": "./dist/worker.d.ts",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck:tests": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@remote-hands/control-plane": "0.0.0",
    "@remote-hands/shared": "0.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  }
}
